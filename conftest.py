import sys
import pathlib



# TODO: can this file move into `tests`


# Ensure the project root is on sys.path so EEequipment and other packages import correctly
sys.path.insert(0, str(pathlib.Path(__file__).parent))
